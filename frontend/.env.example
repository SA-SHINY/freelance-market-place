# Leave blank in local development — Vite's dev server proxies /api and
# /socket.io to http://localhost:5000 automatically (see vite.config.js).
# Set these only when pointing the built frontend at a deployed backend.
VITE_API_URL=
VITE_SOCKET_URL=
